name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v1
    
    - name: configure
      run: cmake -Htest -Bbuild/test
      
    - name: run
      run: CTEST_OUTPUT_ON_FAILURE=1 cmake --build build/test --target test
