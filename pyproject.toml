[project]
name = "cpm-cmake"
version = "0.1.0"
requires-python = ">=3.14"
description = "This project is used with uv to lock Python requirements used in CPM.cmake"
dependencies = [
    "clang-format>=14.0.6",
    "cmake-format>=0.6.11",
    "pyyaml>=6.0.3",
]
